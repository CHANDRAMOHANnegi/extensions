{
  "name": "@extensions-workspace/root",
  "private": true,
  "scripts": {
    "dev:focus": "echo 'Vanilla extension, no build step needed. Load apps/focus directly in Chrome.'",
    "build:all": "pnpm -r --filter=!@extensions-workspace/root build"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
